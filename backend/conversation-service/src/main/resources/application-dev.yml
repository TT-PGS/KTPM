spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Conversation_db?useSSL=false&serverTimezone=UTC
    username: root
    password: a
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
